// Since everything is already configured in the root build.gradle.kts,
// and this service doesn't have any specific dependencies or configurations,
// we can leave this file nearly empty.

// Empty plugins block is still needed to properly apply the plugins
plugins {
}